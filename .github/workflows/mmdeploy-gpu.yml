name: fe-test
on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build:
    name: build
    uses: ./.github/workflows/reusable-build.yml
    with:
      image_name: mmdeploy-gpu
      path: dockerfiles/mmdeploy-gpu
      push_to_ghcr: true
      max_space: true